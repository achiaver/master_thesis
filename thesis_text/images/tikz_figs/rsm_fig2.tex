\begin{figure}[h!tbp]{\textwidth}
    \centering
    \caption{REPLICATED SOFTMAX MODEL}%
    \label{fig:rsm-fig2}%
    \begin{tikzpicture}[%
        visible/.style={draw, circle, thick, on grid, align=center, text width=1cm, inner sep=0pt},
    hidden/.style={draw, circle, thick, on grid, align=center, text width=1cm, inner sep=0pt, fill=gray}]

%        \draw[help lines] (-5,-7) grid (5,4);

% k = 1 and all D words
        \node [visible] (v13) {$v_{13}$};
        \node [visible, right=4cm of v13] (v1K) {$v_{1K}$};
        \node [visible, left=2cm of v13] (v12) {$v_{12}$};
        \node [visible, left=2cm of v12] (v11) {$v_{11}$};
        \path (v13) -- (v1K) node [midway, sloped] {$\dots$};

% k = 2 and all D words
        \node [visible, below=2cm of v13] (v23) {$v_{23}$};
        \node [visible, right=4cm of v23] (v2K) {$v_{2K}$};
        \node [visible, left=2cm of v23] (v22) {$v_{22}$};
        \node [visible, left=2cm of v22] (v21) {$v_{21}$};
        \path (v23) -- (v2K) node [midway, sloped] {$\dots$};

% k = K and all D words
        \node [visible, below=4cm of v23] (vD3) {$v_{D3}$};
        \node [visible, right=4cm of vD3] (vDK) {$v_{DK}$};
        \node [visible, left=2cm of vD3] (vD2) {$v_{D2}$};
        \node [visible, left=2cm of vD2] (vD1) {$v_{D1}$};
        \path (vD3) -- (vDK) node [midway,sloped] {$\dots$};

% k = . . . and all D words
        \path (v21) -- (vD1) node [midway, sloped] {$\dots$};
        \path (v22) -- (vD2) node [midway, sloped] {$\dots$};
        \path (v23) -- (vD3) node [midway, sloped] {$\dots$};
        \path (v23) -- (vDK) node [midway, sloped] {$\dots$};
        \path (v2K) -- (vDK) node [midway, sloped] {$\dots$};

        \node [hidden, above left=3cm and 1cm of v13] (h2) {$h_{2}$};
        \node [hidden, right=4cm of h2] (hF) {$h_{F}$};
        \node [hidden, left=2cm of h2] (h1) {$h_{1}$};
        \path (h2) -- (hF) node [midway, sloped] {$\dots$};

        \begin{scope}[on background layer]
            \node [draw, very thick, rounded corners=10pt, fit=(v11) (v1K) (vDK) (vD1)] (visible) {};
    
            \node [draw, very thick, rounded corners=10pt, fit=(h1) (hF), fill=lightgray] (hidden) {};
            
            \path [draw, very thick] (visible) -- (hidden) node [midway, right] {$\mathbf{W}$};
    
            \node (visiblelabel) [left=of visible] {$\mathbf{V}$};
            \node (hiddenlabel) [left=of hidden] {$\mathbf{h}$};
        \end{scope}

%        \foreach \x in {1,...,9}{%
%            \foreach \y in {1,...,5}{%
%                \draw[thick] (A\x) -- (B\y);
%            }
%        }
%        \foreach \x in {1,...,5}{%
%            \foreach \y in {1,...,5}{%
%                \draw[thick] (B\x) -- (C\y);
%            }
%        }
%        \draw (A5) -- node[right] {$\mathbf{W}$} (B3);
%        \draw (B3) -- node[right] {${\bf W}_2$} (C3);
    \end{tikzpicture}
    \legend{%
        Replicated Softmax model. 
        The top layer represents the hidden layer, denoted by vector $\mathbf{h} = (h_{1}, h_{2}, \dots, h_{F})$, of stochastic binary units. 
        The bottom layer, denoted by matrix $\mathbf{V}$, represents the visible layer with units $v_{ik}$, $i = 1,\dots,D$ and $k = 1,\dots,K$.
        Between visible and hidden units are the weights $w_{ijk}$, which is a symmetric connection, denoted by tensor $\mathbf{W}$.
    }%
    \source{Author}
\end{figure}




